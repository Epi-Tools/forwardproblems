import template from './pools.html';
import controller from './pools.controller';
import './pools.scss';

let poolsComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default poolsComponent;
